# `make` (1) Command

GNU make utility to maintain group of programs

## Installation

### Ubuntu Server

```sh
sudo apt install make
```

## Examples

### Specify a make file

```sh
make -f %make-file%
```